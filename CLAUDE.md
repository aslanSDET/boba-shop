<!--
  Claude Code reads CLAUDE.md automatically; other agent tools read AGENTS.md.
  Rather than keep two copies in sync, this file imports the other one, so both
  see the same rules and there is a single place to edit.

  Put project guidance in AGENTS.md, below its generated Next.js block.

  Do not delete AGENTS.md. `next dev` maintains a generated block in one of
  these two files, and when AGENTS.md is absent it writes that block in here
  instead — and this file's `@AGENTS.md` import would then resolve to nothing,
  taking every project rule with it.
-->

@AGENTS.md
